// Placeholder React App
import React from 'react';

export default function App() {
  return (
    <div style={{padding: 20}}>
      <h1>Password Strength — Demo UI (placeholder)</h1>
      <p>This UI will call the WASM or API to evaluate passwords.</p>
    </div>
  );
}
